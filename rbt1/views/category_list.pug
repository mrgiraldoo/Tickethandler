extends layout

block content
    h1= title

    ul
        each ticketCategory in ticketCategory_list
            li
                a(href=ticketCategory.url) #{ticketCategory.name}

        else
            li There are no ticket categories.